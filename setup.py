from setuptools import setup, find_packages

setup(
    name="OctoPrintMonitor",
    version="0.1",
    description="OctoPrintMonitor allows you to monitor 3D print jobs and display the status on the LCD",
    packages=find_packages()
)
